package notify.alignments;

public enum VerticalAlignment {
    TOP(0), BOTTOM(1), CENTER(2);

    private int alignValue;

    VerticalAlignment(int alignValue) {
        this.alignValue = alignValue;
    }
}
